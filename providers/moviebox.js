// Provider adapter placeholder.
// Add only sources/APIs that you own or are authorized to use.

export async function catalog({ type, id, extra }) {
  return [];
}

export async function meta({ type, id }) {
  return null;
}

export async function streams({ type, id }) {
  return [];
}
